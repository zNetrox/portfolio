---

---

<nav>
    <div class="container">
        <div id="links-list">
            <a class="slide-up delay-02" href="#">accueil</a>
            <a class="slide-up delay-01" href="#">compétences</a>
            <a class="slide-up" href="#">projets</a>
        </div>
        <div id="contact">
            <p class="slide-left"><span>contact: </span>enzo.galiano38@gmail.com</p>
        </div>
    </div>
</nav>

<style>
    nav {
        position: fixed;
        top: 0%;
        transform: translateY(-100%);
        width: 100vw;
        height: 100vh;
        background: var(--red);
        color: var(--white);
        transition: 1.4s var(--bezier);
    }

    nav.active {
        transform: translateY(0);
    }

    .container {
        position: relative;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
        width: 94vw;
        height: 100%;
    }

    #links-list {
        display: grid;
        grid-template-rows: 33% 33% 33%;
    }

    a {
        width: min-content;
        font-weight: 800;
        font-size: 175px;
        color: inherit;
        text-transform: uppercase;
        user-select: none;
    }

    a:hover {
        font-style: italic;
    }

    a:hover span {
        font-style: oblique;
    }

    #contact {
        font-size: 20px;
        position: absolute;
        bottom: 20px;
        right: 0;
    }

    #contact span {
        font-weight: 800;
    }

</style>